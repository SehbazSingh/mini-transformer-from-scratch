# mini-transformer-from-scratch
A lightweight implementation of a self-attention based language model in PyTorch, featuring custom embeddings, positional encoding, and next-word prediction with sequence modeling.
